{% extends 'base.html.twig' %}

{% block body %}

<article class="row">
                <div class="col-md-6">
                    <h1>{{ pagina.titulo }}</h1>
                    <div class="separador"> </div>
                    <h2>{{ pagina.subtitulo }}</h2>
                    {{ pagina.texto | raw }} 
                </div>
                <div class="col-md-6">
      
                </div>
            </article>

{% endblock %}
