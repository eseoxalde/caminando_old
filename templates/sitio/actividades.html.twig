{% extends 'base.html.twig' %}

{% block body %}

<article class="row">
                <div class="col-md-6">
                    <h1>{{ titulo }}</h1>
                    <div class="separador"> </div>
                    <h2>{{ subtitulo }}</h2>
                    {{ texto }}
                </div>
                
        
            </article>

{% endblock %}
