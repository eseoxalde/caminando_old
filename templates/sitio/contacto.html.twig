{% extends 'base.html.twig' %}

{% block body %}

<article class="row">
                <div class="col-md-6">
                    <h1>{{ titulo }}</h1>
                    <div class="separador"> </div>
                    <h2>{{ subtitulo }}</h2>
                    {{ texto }}
                </div>
                <div class="col-md-6">
                    {% if imagen_tipo1 and ruta_imagen_tipo1 is not empty %}
            <div>
            <img src="{{ asset(ruta_imagen_tipo1) }}"  alt="Foto de perfil predeterminada" class="img-fluid rounded-circle" width="150" height="150">
            </div>
        {% endif %}
                </div>
        
            </article>

{% endblock %}
